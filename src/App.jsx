import React from "react";

export default function App() {
  return (
    <div style={{ padding: 24, color: "#e8e8e8", background: "#121212", minHeight: "100vh" }}>
      <h1>Astrocusp web is running</h1>
      <p>If you see this on Netlify, the build is wired correctly.</p>
    </div>
  );
}
